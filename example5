include<stdio.h>
int main()
{
int a,b,c;
scanf("%2d%2d",&a,&b);
printf("numbers a&b are:%d",a,b);
c=a+b;
printf("sum of a&b is:%d",c);
c=a-b;
printf("substraction of a&b is:%d",c);
c=a*b;
printf("multiplication of a&b is:%d",c);
c=a/b;
printf("division of a&b is:%d",c);
return 0;
}
